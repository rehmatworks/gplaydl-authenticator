<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing is backed up: the API key and the device secret must never leave
     the device they were issued to. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
